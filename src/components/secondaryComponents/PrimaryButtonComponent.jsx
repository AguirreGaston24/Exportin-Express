import React from "react";

export const PrimaryButtonComponent = () => {
  return (
    <button
      style={{
        width: width,
        height: height,
        fontSize: fontSize,
        backgroundColor: backgroundColor,
      }}
    ></button>
  );
};
